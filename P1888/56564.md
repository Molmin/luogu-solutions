因为是求较小锐角的正弦值(sin)，画个图就可以知道是较短的直角边与最长的边（斜边）的比值 其他马子什么的都不用想了（什么快排什么python（不懂），不存在的）那就好做了。

由于问题在Rt三角形中开展，所以只需排序一下确定直角边与斜边，用较短的直角边去除斜边即可，重点在于约分，这里使用辗转相除法求最大公约数。

思路分析：

①因为本题数据不一定是按从小到大的顺序输入的，所以在输入后必须进行判断，找出其中的最大值和最小值；②在找出最大值和最小值后，由于题目中要求约分，我们可以通过递归辗转找到它们的公因数，并将最大值和最小值分别去除以公因数；③最后按照一定的格式将正弦的值输出。

自定义狂人来了

||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||||
```cpp
#include<cstdio>
#include<iostream>
#include<cmath>
#include<algorithm> 
#define z (scanf("%d%d%d",&a[0],&a[1],&a[2]))//输入三条边
#define w (sort(a,a+3))//将三边排序，其中a[0]为较短直角边，a[2]为斜边
#define k (i=a[0]/s(a[0],a[2]))//这是最小的数
#define p (j=a[2]/s(a[0],a[2]))//这是最大的数，也就是说，斜边
#define l (printf("%d/%d",i,j))//无脑输出
#define r (y==0?x:s(y,x%y))//辗转相除法，三目运算符，y=0时return x否则  return s（y，x%y）递归递归
using namespace std;
int a[3],n,i,j;
int s(int x,int y)//求最大公约数，辗转相除法，基本基本
{
    return r;
}
int main()
{
    z,w;
    k;p;l;//怪我咯
    return 0;//结束
}
```