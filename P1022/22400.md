此题难度不高（约pjT2【估错了别怪我），主要用移项法

###但是，神坑。

下面简单谈一谈我的心酸史

No.1 误以为未知数只可能为a（其实a~z中任意一个都行，题目只保证一个方程中只有一个字母）——0分（3RE【200】，2wa）；

No.2对于我的算法字符串末尾应该再加一个运算符（但是我忘了，555.。至于为什么，程序里讲）

No.3 没考虑系数为1时，系数可以省略（如最后一个点）

以上已删去各种本蒟蒻犯的各种手残错误

扯多了，谈正事

------------------------------------------------------我是华丽的分割线---------------------------------------------------------

```cpp
var
s,s1:string;
x,fu,na,ns,i,len,fux:longint;
hh:char;
begin
readln(s);
len:=length(s);
s:=s+'+';
i:=1;
x:=0;//用来统计常数项，系数
fu:=1;//标记加减
fux:=1;//标记在等号的左右位置（移项变号，这个大家应该都知道）
while i<=len do
begin
if s[i]='+' then fu:=1*fux
  else if s[i]='-' then fu:=-1*fux//具体处理加到累加器上的符号
  else if s[i]='=' then
  begin
  fu:=-1;
  fux:=-1;
  end//等于是换号
  else
    begin
    if s[i] in ['0'..'9'] then//s[i]是数字，集合操作，等价于 (s[i]>='0')and(s[i]<='9')
    x:=x*10+ord(s[i])-48;
    if s[i+1] in ['a'..'z'] then//s[i+1]是字母，同上
    begin
    if x=0 then x:=fu;//处理系数为1的情况
    na:=na+x*fu;//na是字母系数累计器
    fu:=0;
    inc(i);
    hh:=s[i];//记录未知数是什么
    x:=0;
    end
    else
    if (s[i+1]='+')or(s[i+1]='-')or(s[i+1]='=') then//下一个是符号，代表上面的是常数项（字母情况上面已经做了，这里是else）因为我检验的是i+1所以如果给定方程末尾为常数项，那么字符串末尾不加运算符会导致这个项不被计入，导致错误
    begin
    ns:=ns+x*fu;//ns为常数项累加器
    x:=0;
    fu:=0;
    end;
    end;
inc(i);
end;
write(hh,'=',-1*ns/na:0:3)//要*（-1），因为我这里上面的操作可以认为是将所有项移到左边，这里要把常数项移到右边
end.
以上方法仅供参考，若有疏漏，请谅解！
```