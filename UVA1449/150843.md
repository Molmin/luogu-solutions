考虑最朴素的暴力，枚举每个模式串并在文本串里面直接比较，复杂度 $O(n|S||T|)$。

可以用哈希或 kmp 优化直接比较的复杂度，复杂度变为 $O(n|T|)$。

但也可以枚举 T 的长度为 $1\sim|S|$ 的子串，开个桶存下它们的哈希值，对每个模式串查询其哈希值的出现次数，复杂度 $O(|S||T|)$。

在本题中第二种写法比第一种更快。

[Code](https://www.luogu.com.cn/paste/tha1mupj)