一道较为简单的模拟。

题意：给定几个整数，找出最小的形如 $111...111$ 的整数 y 满足 $a\bmod b=0$，求 y 的位数。

思路：先令 $y=1$，$ans=1$，每次判断 $y\bmod x$ 是否为 0，
如果是的话输出 ans，否则 y 的位数加一，即 $y=y*10+1$ 。

细节：在计算过程中 y 可能会很大，所以需要随时取模。

```cpp
while(cin>>x)
{
	ans=1;
	y=1;
	while(y%x!=0)
	{
		ans++;
		y=(y*10+1)%x;
	}
	cout<<ans<<endl;
}
```
