不解释，直接上代码：

```cpp
#include<bits/stdc++.h>
using namespace std;
int main()
{
    int len;
    char a[1001];
    cin>>a;
    len=strlen(a)-2;
    cout<<a[0]<<len<<a[strlen(a)-1]<<endl;
    return 0;
}
```

# 别忘了换行! ! !