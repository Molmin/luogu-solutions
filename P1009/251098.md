# 今天，我终于吧递归给学会了！！[faceclap]

```
d~e~f f~a~c~t~(~x~)~:定义乘阶函数
    i~f ~x =~= ~1~:特判
        re~turn 1;
    else:
        re~turn x *~ fact(x - 1)#调用自己(
~Sum = 0
fo~r i in~ r~ange(1, int(inpu`t()) + 1):#我不友善，真的！
 ~   ~Su~m += fac~t(i)
pr~in~t(~S~u~m~)输出
```
来，讲解一下这整个代码的套路：

从样例入手：

输入 $3$

---

$for$ 从 $1$ 开始：

$1$ 返回 $1$，$1 = 1$

---

$for$ 循环到 $2$

从 $2$ 开始：

$2$ 返回 $2$ $*$ $（2 - 1）(即x - 1)$ 

$2 - 1 = 1$ 

$1$ 返回 $1$，$2 * 1 = 2$

---

$for$ 循环到 $3$

$3$ 返回 $3$ $*$ $（3 - 1）(即x - 1)$

$3 - 1 = 2$ 

$2$ 返回 $2$ $*$ $（2 - 1）(即x - 1)$

$2 - 1 = 1$ 

$1$ 返回 $1$，$3 * 2 * 1 = 6$

样例答案：

$1 + 2 + 6 = 9$

你看得可能会有点晕。

我给你再简洁一点：

```python
def coutdown(x):
	print(x)
	coutdown(x - 1)
```
调用这个代码，你会发现他停不下来，然后~~关机吧~~按$Ctrl + C$就可以停下来

这就是递归：递归就是调用自己的函数

它的作用和循环差不多，但递归有基线条件和递归条件，在这点上~~我认为~~递归比循环好用，但递归会调用栈，栈会越用越多，运气~~好~~的话--->蓝屏警告，在这点上循环不会占用更多的空间。

好了，~~小葵花妈妈~~我的课堂结束了。愿管理员大大能通过！