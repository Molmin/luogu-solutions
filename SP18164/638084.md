## 分析

考虑直接暴力。

这道题我们可以考虑直接暴力，循环枚举 $t$ 的值，每次 $n \to n \cdot k$，当 $n > m$ 退出循环，输出 $t$。如果 $t$ 是从 $0$ 开始枚举的则直接输出，从 $1$ 开始的则需输出 $t - 1$。

时间复杂度为 $\mathcal{O(\log_k \frac{m}{n})}$，$2 \le n,m,k \le 10^{18}$，不会超时。

注意：此题应使用 `long double`。

代码请读者自行实现。