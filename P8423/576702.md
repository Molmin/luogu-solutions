简要题意：输出长颈鹿在风井省参加的比赛的缩写。

例：真卷省举办的省级选拔赛的缩写为 `ZJOI`。

可以发现，真卷省的缩写为 `ZJ`，再加上想象学竞赛的后缀 `OI`，即为 `ZJOI`。

以此类推，风井省的缩写为 `FJ`，再加上想象学竞赛的后缀 `OI`，即为 `FJOI`。

此题的输入对于解题没有意义，所以可以不用读入。

```cpp
#include<bits/stdc++.h>
using namespace std;
int main()
{
	puts("FJOI");
	return 0;
}
```