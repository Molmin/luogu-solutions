### 题意：
你有价值$1!,2!,3!......15!$的钞票，询问你要购买价格为$n$的物品时最少要花多少张钞票。

### 思路：
我们先算出所有的阶乘
```
int f[16]{1};
//1的阶乘为1
for(int i=1;i<16;i++)
	f[i]=n*=i;
```
然后计算用的钞票总数
```
int num=0;
//初始化
for(int i=15;n>0&&i>0;n%=f[i--])
//每次都模
{
 	num+=n/f[i];
	//加上它的倍数
}
cout<<num<<endl;
//换行别忘记了噢
```

然后就结束啦~