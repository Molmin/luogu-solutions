这道题很水，按照翻译模拟即可。
```cpp
#include<bits/stdc++.h>//头文件
using namespace std;
int n,m;
int main(){
    cin>>n>>m;
    cout<<((m==1)?1:0)+1<<endl;//输出答案
    /*
    问号运算符：
    当括号里的式子符合时，返回冒号前的值。
    否则，返回冒号后的值。
    */
    return 0;//结束
}
```