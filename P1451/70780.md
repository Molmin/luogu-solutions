算法前面的dalao已经说得很清楚了，这里，我给大家一种用int数组解题的方法   
用scanf读入整数的时候可以控制读入的位数，比如```
scanf("%2d",&m);
```就是读入一个2位的整数   
所以可以用以下方式读入整个矩阵   
```cpp
for(c=1;c<=m;c++)//循环变量稍微有点奇怪   
{   
	for(d=1;d<=n;d++)   
	{   
		scanf("%1d",&mapp[c][d]);   
	}   
}   
```