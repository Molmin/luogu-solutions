## 题目翻译
多组数据，每次输入 $n$，以 $0$ 结束，每次判断如果 $n\bmod 17$ 为 $0$，输出 `1`，否则输出 `0`。

$1\le n\le 10^{100}$。
## 题目思路
由于 $n$ 的范围很大，所以可以用 python 来写程序。

其他就不用说了，很简单。
## 代码
```python
while True:
    n=int(input())#输入
    if n==0:exit(0)#n为0结束程序
    if n%17==0:print(1)#判断
    else:print(0)
```
