## 说明 解析P1060 【开心的金明】
~~（参考了一下题解）~~

个人思路：首先，有些人一看到这种题目就用贪心，所谓的贪心策略是：让价格乘以价值的乘积大的就买，然而，大家思考一下，这样做对吗？
- 就拿样例举例

价格 价值 乘积
1. 800 2 1600
1. 400 5 2000
1. 300 5 1500
1. 400 3 1200
1. 200 2 400

**这样是不行的！！**
- 由于是乘积最大的先买，所以是先买第二个物品，然后再买第一个物品，然而，买了第一件物品后，第二件物品就买不了了，因为超过给定的钱数了，所以这个方法得到的结果是400，离3900差的太远了！本题应该使用动态规划！
- 接下来就是状态转移：就是前一个的状态与后一个的状态（后一个商品的价格乘以价值）进行比较，取最大值这样就能包揽全局了。

###   上代码~
```
#include<bits/stdc++.h>    
using  namespace std;         
int C[30010],s[30],s1[30];//s1代表价格，s代表价值
int main()
{
    int n,m,i,j;
    cin>>n>>m;
    for(i=1;i<=m;i++)
    {
        scanf("%d%d",&s[i],&s1[i]); //输入每个商品的价格和价值
    }
    for(i=1;i<=m;i++)
    {
        for(j=n;j>=s[i];j--)
        {
            C[j]=max(C[j],C[j-s[i]]+s1[i]*s[i]);//状态转移（参考题解的部分）但是很重要！！！
        }
    }
    cout<<C[n];//输出 
    return 0;
} 
```

谢谢各位大佬对本人的关心！！！
