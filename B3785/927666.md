Test 1:
通过观察题意，可知 $d=b-c$，可以发现这个测试点是向下取整（因为只要除数小于$0$就为默认输出$0$），正确的运算方法应该用 ceil 函数，所以只要使 $b - c$ 等于负数，就行了。
代码如下：

```
if (taskId == 1) {
    cout << "2 2 3" <<endl;
 }
```
Test 2:
因为每逢到```std::cerr```就将 ans 加一并输出，所以
这个点只要开个循环（开到 $2\times10^6+2$ 就行了）使其输出超时就行了（我比赛竟然没想起来）。代码如下：
```
else if (taskId == 2) {
  	cout << "200002"<<endl;
for(int i=1;i<=200002;i++)puts("std::cerr");
    } 
```
