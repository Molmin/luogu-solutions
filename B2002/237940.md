本题叫我们直接输出字符串 `Hello,World!`，都不用输入

于是我们就按照题意直接输出就行了

代码（包括注释）：

```cpp
#include <iostream> //包含流输入和流输出的头文件
using namespace std; //使流输入和流输出能够正常使用
int main() {
  cout << "Hello,World!" << endl; //输出字符串 "Hello,World!"，其中双引号内可输出其他字符
  return 0; //结束主程序
}
```
