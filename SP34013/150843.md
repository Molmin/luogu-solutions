本题思路：

一、按题意输入

二、因为要使取的石头最少，所以把石头的重量从大到小排列

三、每次取完石头后判断水是否溢出

四、溢出就输出，没溢出继续累加

本题**精髓**：
```cpp
for(i=1;i<=n;i++){
    s+=m[i];  //累加
    if(s+d>c){  //判断
        cout<<i<<endl;  //记得换行
        break;  //因为找到了，所以不用再往下找，直接跳出循环
    }
}
```