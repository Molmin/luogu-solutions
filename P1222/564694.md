# 分析：
乍一看这题很简单，直接减掉重复部分就行了，但这样只能得 70 分。

因为根据容斥原理，重叠三层的还得加回去。

所以，算法就是这样的：

算出现有三角形的总面积，将答案加上 $(-1)^{x + 1} \times S$ (其中 $x$ 表示重叠的层数，$ S $ 表示现有三角形的总面积)

再把现有三角形重叠的三角形进入下一次操作，直到没有重叠部分。

在极端情况下，这种算法的复杂度是 $O(n^3)$ , 但在随机数据下可以放心使用。