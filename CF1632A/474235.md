### 简略题意：

请问能否通过任意排列字符串的方式使一个只由 `0` 和 `1` 组成的字符串所有字串没有长度 $≥ 1$ 的回文子串。

-----

### 分析

很明显，当字符串长度 $> 2$ 时，由于 `0` 或 `1` 中至少有一种个数 $> 1$，所以总是有至少一种长度 $> 1$ 的回文子串。

所以很容易得出结论：

- 当长度 $≤ 2$，有解

- 反之，无解。

这么简单，代码自己写吧，不超过 5 行。

对于蒟蒻：这里有一份[压行代码](https://www.luogu.com.cn/paste/7j52quzg)，可以参考