## Python会TLE...(っ °Д °;)っ
### PyPy3会UKE...(;´༎ຶД༎ຶ`)
#### ~~虽然PyPy2可以~~
# 所以我用Ruby！！！
Ruby中如果数大于$2^{62}-1$则会转化成Bignum类型   
具体有多大就得取决于你的计算机内存了~
```cpp
n=gets.to_i #输入数据组数
for i in 1..n #循环
    a, b = gets.split.map(&:to_i) #输入a和b
    print a*b #输出a*b
    puts("\n") #换行
end #华丽的结束~
```   
~~这题昨天还是黑的啊啊啊啊~~   
~~Ps：Ruby比PyPy2快多了~~   
~~PPs:To管理员大大：这次能让我过了吗？？？~~