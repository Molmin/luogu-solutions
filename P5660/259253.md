## 今天在考场上，我惊呆了
####              因为太简单了。。。。
贴一份通俗易懂的代码


------------------------------------------------分割线------------------------------------------------

```cpp
#include<bits/stdc++.h>//万能头文件(guide能用)
using namespace std;
int main()
{
	string a;//字符串
	int b=0;
	cin >> a;
	for (int i=0;i<8;i++)//这题竟然固定了长度
		if (a[i]=='1')//string用法之一，提取下标为i字符      
			b++;
	cout << b;
	return 0;
}
```

