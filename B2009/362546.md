这题和 B2008 一样，都是供萌新来练习的题。

先输入 $a$，$b$，$c$ 三个数字。

然后输出 $( a + b ) \div c$ 的值即可。

### 代码

```
#include<iostream>
using namespace std;
int a,b,c;
int main() 
{
	cin>>a>>b>>c;
	cout<<(a+b)/c<<endl;
	return 0;
}
```