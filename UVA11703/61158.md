这题是一道简单的递推题（~~感觉这题最大的难点在读懂题目~~）。题意其他的题解都说的很清楚。

因为这题用到了三角函数，所以只要导入cmath库，然后就按照题意进行模拟就行了。这题要用到的函数如下：
```cpp
sqrt(x) //求x的平方根
ln(x) //求x的自然对数
sin(x) //珂爱的正弦
```
对于这道题的向下取整，有两种方法。
```cpp
floor(x) 或者 int(x)
```
其实强制类型转换是**向零取整**，floor才是向下取整，只是这题数据水，都能过。

掌握了上面的函数后，只用按照题意递推就行了（~~代码甚至都不用放~~）。
