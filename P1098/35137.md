终于AC啦！！！

迟来的一篇题解...

一开始没看见题目里的必须前后同为字母或数字才能展开，还得我70了N次，发了好几个贴求助居然没人回

\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

@楼上各位：不要把代码打那么长好吗？！你们写着不累吗？！

\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_\_

编几个函数处理一下不就行了

具体解释见代码：

—————————————————————————终于开始了—————————————————————————————

```cpp
#include <iostream>//cin，cout
#include <cstring>//求字符串长度，同时包含cctype（toupper转大写,toloewr转小写,isalnum是字母或数字,isslpha是字母）
#include <cstdio>//printf
using namespace std;
string a;char b[20010];//输入，输出（当时做时是半年前，不知道string为什么附不了值，就用了char*）
int i,len,p1,p2,p3,k=0;//len，长度；k，当前输出串长度
char j;
void pp2 (char c)//处理p2
{
    for (int ppp2=0;ppp2<p2;ppp2++) b[k++]=c;//连续输出p2个c
}
void pp1 ()//处理p1
{
    if (p1==1) pp2 (tolower(j));//如果是1，填充大写字母（数字转大小写不变）
    else if (p1==2) pp2 (toupper(j));//2，填充小写
    else pp2 ('*');//3，填充*
}
void pp3 ()//处理p3
{
    if (p3==1)//是1，顺序填充
    for (j=a[i-1]+1;j<a[i+1];j++) pp1 ();
    else//逆序
    for (j=a[i+1]-1;j>a[i-1];j--) pp1 ();
}
int main ()
{
    cin>>p1>>p2>>p3;//输入
    getline (cin,a);//读'\n'
    getline (cin,a);//输入
    len=a.size();//求长度
    for (i=0;i<len;i++)
    {
        if(i>0&&i<len-1&&a[i]=='-')//非第一个和最后一个，且为-
        if(a[i-1]<a[i+1]//前一个小于后一个
              &&(isalnum(a[i-1])&&isalnum(a[i+1]))//且都是字母或数字
                   &&(isalpha(a[i-1])==isalpha(a[i+1]))) //这时，不是字母，就是数字，都是字母（1,1；1==1），都是数字（0,0；0==0）
        pp3 ();//全部成立，执行展开操作
        else b[k++]=a[i];
        else b[k++]=a[i];                                          //否则，原样输出
    }
    b[k]='\0';
    printf ("%s",b);//输出
    return 0;
}
```
简单易懂，共42（含头文件）行
