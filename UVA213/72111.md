这个题样例说明了一个问题，编码头是可以有空格的，相信大家都是从刘汝佳神犇那里看到的这个题，那个题解抄过来以后样例过不了。需要加几个字母，就是特判一下空格。希望注意。

```
inline int readchar() {
    while (1) {
        int ch = getchar();
        if( ch != '\n' && ch != '\r'&&ch!=' ') return ch ;
    }
}
```