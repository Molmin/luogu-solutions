水题。

总共也就 `00`，`01`，`10`，`11` $4$ 种情况。

而且 `00` 没有贡献。

设 `01` `10` `11` 的最小时间为 $a,b,c$。

`01` 和 `10` 可以合起来，时间 $a+b$。

`11` 可以单独，时间 $c$。

所以答案是 $\min(a+b,c)$。

注意当 $c$ 不存在且 $a,b$ 中有一个不存在时无解。