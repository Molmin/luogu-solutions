这题不是很难，只要会语言的都会写

由于在编程中，没有```×```这个符号，我们会使用```*```。

所以就是要计算 ```(a+b)*c```

代码：
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
	short a,b,c;
	cin>>a>>b>>c;
	cout<<(a+b)*c;
	return 0;
}
```