这题是一道供刚入坑的萌新来练习的题。

先输入 $a$，$b$，$c$ 三个数字。

然后输出 $( a + b ) \times c$ 的值即可。

### 代码

```
#include<iostream>
using namespace std;
int a,b,c;
int main() 
{
	cin>>a>>b>>c;
	cout<<(a+b)*c<<endl;
	return 0;
}
```