这是一道入门题目，初学者专用

### 定义变量
说的很明确，定义 $a$ , $b$ , $c$。
### 输入
定义了 $a$ , $b$ , $c$ ,肯定就要输入 $a$ , $b$ , $c$。
### 输出
输出$(a+b)\times c$。
### 代码
```
#include<iostream>
using namespace std;
int main(){
    int a,b,c;
    cin>>a>>b>>c;
    cout<<(a+b)*c<<endl;
}
```