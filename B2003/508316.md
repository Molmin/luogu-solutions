我们可以按照题意模拟。

输入 $a,b,c$ 三个数后，将 $b$ 输出。

代码如下：
```
#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;
int main() {
	int a, b, c;
	cin >> a >> b >> c;
	cout << b;
	return 0;
}
```
