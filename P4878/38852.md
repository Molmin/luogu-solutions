此题差分约束系统，
同时据题意奶牛顺序必须从左到右

设1号奶牛为x1，
2号奶牛为x2
以此类推
 
奶牛间距离不得小于或大于某个值相当于将这个题转化为：
有若干个变量、有若干的不等式
根据不等式列出取值范围确定答案

但又要求最后答案为从1到n号点的距离最大 ，因此
转化为最短路问题

我们设s为起点，则
$dis[i]$表示$s$到$i$的最短路

因此不等式可以写成$dis[a]-dis[b] \leq c$;

如果有大于的情况，通过移项，将其变为小于

据此建边连图

跑最短路

得到取值最大范围的值

