题解：

由于数据范围很大($10^{18}$)，所以不能枚举。

很容易得出$n^2<n*(n+1)<(n+1)^2 (n>=1)$

所以就先算出$\sqrt{k*2}$的整数，然后再将这个整数乘上它+1再/2

如果得到的是k，答案就是它了；

反之，没有答案。

```cpp
int main(){
	ll x;
	cin>>x;
	x*=2;
	ll t=sqrt(x);
	if(t*(t+1)==x) cout<<t;
	else cout<<-1;
}
```