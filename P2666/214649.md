你们好暴力啊！

你以为我是dfs？

我也是暴力，嘻嘻

有人说会超时是什么鬼？48ms貌似还行啊

一只代码（带注释

```cpp
#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n,ans=0;//定义一下
	cin>>n;//输入以下
	for(int i=0;i<=sqrt(n);i++)//循环了！，你们100是什么呀？好奇，不是平方吗
	{
		n-=i*i;//一个可爱的偷懒
		for(int j=0;j<=sqrt(n);j++)//循环了！，你们100是什么呀？好奇，不是平方吗
		{
			n-=j*j;//一个可爱的偷懒
			for(int l=0;l<=sqrt(n);l++)//循环了！，你们100是什么呀？好奇，不是平方吗
			{
				n-=l*l;//一个可爱的偷懒
				if(int(sqrt(n))==sqrt(n))//这是平方数吗？//注释中的注释——偷懒有用了！
				{
					ans++;//计数器快加一！
				}
				n+=l*l;//偷完懒再还回去！懒也要还吗？
			}
			n+=j*j;//偷完懒再还回去！懒也要还吗？
		}
		n+=i*i;//偷完懒再还回去！懒也要还吗？
	}
	cout<<ans;输出了QAQ
	return 0;//AC了呢！q(≧▽≦q)
}
```

）

这个题解蒟蒻也能懂哦！