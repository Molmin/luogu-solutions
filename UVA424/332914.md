这题目用 python 做很好，虽然我是c党，但是没有自带高精度表示不服，学了学几个最简单的语句做了个py3代码然后A了：

```
var = 1
ans = 0
while var == 1:
    num = int(input())
    if num == 0:
        break
    else:
        ans = ans + num
print(ans)

```
