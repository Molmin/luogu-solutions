对于自带高精的语言来说，这道题真是简单。

我们可以先定义一个累加器$s$，把它的值设为$0$，然后对于输入的每个数，如果它为$0$就直接跳出循环，输出累加器即可。否则就累加。

上代码。
```python
s = 0

while True:
    a = int(input())
    if a == 0:
        break
    
    s += a
    
print(s)
```