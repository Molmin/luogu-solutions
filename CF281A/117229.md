```pascal
这题……不想说
但高速一下P党，P语言有个upcase函数，可以把一个字符串（或一个字符）转化成大写，比如s:=upcase('w');那么s就等于'S'。
代码：
var
 s:ansistring;
begin
 readln(s);//输入s
 s[1]:=upcase(s[1]);//把第一个字符转化成大写
 writeln(s);//输出
end.

别忘记点赞o(￣▽￣)ｄ
```