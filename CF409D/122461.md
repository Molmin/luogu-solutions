又是一道常识题，只是题目描述有点唬人。

~~很简单的常识，~~不会可以上百度。

之前有过C++的，因此我拿来了一个Python。

```python
ans = ["1", "0", "0", "1", "0", "1", "0", "1", "1", "1", "0", "0", "1", "0", "1", "0"]

n = input()
print(a[n-1])  #下标从0开始
```