这题实在太水了。。。      
费那么大力气判断输入了LR什么干啥~~小蒟蒻也不知道模拟是什么~~，直接接收到操作步骤的数量再+1不就可以了？                            
代码如下
```cpp
#include<cstdio> //头文件
using namespace std; //命名空间，可有可无
int main()
{
	int num;
	scanf("%d ",&num); //输入数量，LR什么的不用管他
	printf("%d",num+1); //直接输出数量+1
}
```
十分简单。。。 
 
  
思路：   
情况一：啥东西都执行不了~~那这是什么lj机器人~~，留在原地   
情况二：只能执行L（或R），这样就能往某一段走最远，即使LR混合执行也只会在有这个范围内  
所以得到算法：操作步骤数（不管左右都行）+1（留在原地）就ok啦  
**~~其他方法真的看不懂。。。~~**