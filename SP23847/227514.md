分类讨论：

设高手数为 $g$，新手数为 $x$。

如果 $2g\le x$，则每个高手都能和两个新手组队，答案为高手数。

如果 $2x\le g$，则每个新手都能和两个高手组队，答案为新手数。

如果 $2x\ge g$ 并且 $2g>x$，则每个人都能互相组队，答案为 $\lfloor\dfrac{x+g}{3}\rfloor$，也可以写成 $\dfrac{(x+g)-(x+g)\bmod 3}{3}$，~~虽然没必要~~。

根据上面的内容，代码也就很好写了，大家可以轻易写出qaq。