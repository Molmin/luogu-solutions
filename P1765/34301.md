**###**###不明白很多人为什么这么老实，真的去模拟。。****

推荐一种方法，case，使劲的打表（我这也是一开始乱想的，但居然真的过了。）

那么神奇的代码如下：

```cpp
var
  s:string;
  s1,i,jlongint;//s1为按键次数
begin
  //assign(input,'1.in');reset(input);
  //assign(output,'1.out');rewrite(output);
  readln(s);
  for i:=1 to length(s) do
    case s[i] of//啊哈，使劲的打表开始
      'a':s1:=s1+1;//如果要打的字母是'a’，那么按键次数就加1（按一次2）
      'b':s1:=s1+2;//加二（按两次2）
      'c':s1:=s1+3;//加三（按三次2）
      'd':s1:=s1+1;//加一（按一次3）
      'e':s1:=s1+2;//加二（按两次3）
      'f':s1:=s1+3;//加三（按三次3）
      'g':s1:=s1+1;//加一（按一次4）
      'h':s1:=s1+2;//加二（按两次4）
      'i':s1:=s1+3;//加三（按三次4）
      'j':s1:=s1+1;//加一（按一次5）
      'k':s1:=s1+2;//加二（按两次5）
      'l':s1:=s1+3;加三（按三次5）
      'm':s1:=s1+1;//加一（按一次6）
      'n':s1:=s1+2;//加二（按两次6）
      'o':s1:=s1+3;//加三（按三次6）
      'p':s1:=s1+1;//加一（按一次7）
      'q':s1:=s1+2;//加二（按两次7）
      'r':s1:=s1+3;//加三（按三次7）
      's':s1:=s1+4;//加四（按四次7）
      't':s1:=s1+1;//加一（按一次8）
      'u':s1:=s1+2;加二（按两次8）
      'v':s1:=s1+3;加三（按三次8）
      'w':s1:=s1+1;加一（按一次9）
      'x':s1:=s1+2;//加二（按两次9）
      'y':s1:=s1+3;//加三（按三次9）
      'z':s1:=s1+4;//加四（按四次9）
      ' ':s1:=s1+1;//再是空格，加一（按一次0）
    end;//以上就是打表的过程（艾玛，居然做死的打完了。。。）
  write(s1);//算出来其实是22，可能最后还有一个空格
  //close(input);
  //close(output);
end.

```