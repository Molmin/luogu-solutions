这里发一篇`Python`的题解。

首先，我们只带最多买的支数是$\lfloor\frac{10a+b}{19}\rfloor$，所以我们读进两个整数，然后按照上面这个公式算即可。

在`Python`中，下取整可以用`//`表示（否则自动输出浮点数），然后输入有一些小技巧。代码如下（可能`//`会被当成注释，但不影响编译）：
```python
s=input().split()
a=int(s[0])
b=int(s[1])
print((a*10+b)//19)
```