### 这是一个python题解  
##
### 题面：
Ilya是一只灰常聪明的狮子，他住在一座不同寻常的城市中——动物园！在这座城市中，每一只动物都有它的权利与义务。它们甚至都有自己的银行账户。银行账户包含一个整数，其中有可能是负数（说明它欠钱了）。

Ilya最近要过生日了，所以他收到了一系列礼物。其中一个礼物（动物园总行）可以让它**消除它银行账户上的最后一位或倒数第二位数字**（就一次）。  
  
  比如说，如果Ilya的账户上余额为 $-123$，那么他可以消除 $3$ 得到 $-12$，或者消除 $2$ 得到 $-13$（当然它不傻，知道欠钱越少越好）。**Ilya也可以不消除数字。**

现在，Ilya要你帮他算出它**最多**有多少钱。  
##  
### 思路：  
如果输入的数是正数，无论消除哪一个数字，都只会让钱变少，所以选择不消数字。  
如果输入的数是负数，就消除倒数两位之中数字更大的一个，注意如果消除后数为  $0$需要特判（如 $-10$ 消除后为 $-0$ 即 $0$）。  
如果输入的数是 $0$，不用操作。  
  
注释里也有解释。  
##
### 代码：  
```python
import sys #头文件
a=input()  #输入的是字符串
b=""       #记录答案用的字符串
if int(a)>=0:
    print(a)     #正数直接输出
    sys.exit(0)  #跳出程序
if int(a[len(a)-1]) >= int(a[len(a)-2]):  #如果倒数第一位大于等于第二位，则舍去倒数第一位
    for i in range(len(a)-1):
        b+=a[i]
else:                                     #倒数第二位大于倒数第一位
    for i in range(len(a)-2):             #加到倒数第三位
        b+=a[i]
    b+=a[len(a)-1]                        #单独加上最后一位
print(int(b))                             #转换成整型后输出

```  
### AC记录：  
[here](https://www.luogu.com.cn/record/39903981)