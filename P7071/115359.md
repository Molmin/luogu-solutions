用lowbit即可 。

lowbit(x)=x&(-x)是x的二进制表达式中最低位的1所对应的值 。

常用于树状数组 。

利用负数的补码的特性，方便又好记 。

```cpp
    while(a)
    {
        cout<< (a&(-a)) <<' ';
        a-=a&(-a);
    }
```
会发现是从小到大，题目要求从大到小 。

用数组存了反向输出即可 。

作为T1还行 。
