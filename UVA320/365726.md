### 思路：
无脑模拟，分为以下几个步骤：

$step$ $1$：初始化地图每一个点为```.```。
```
void init() {
	for(int i=0;i<=31;i++)
		for(int j=0;j<=31;j++) 
			M[i][j]='.';
}
```



------------


$step$ $2$：处理第几个地图，注意换行。
```
cout<<"Bitmap #"<<cnt<<endl;

```



------------


$step$ $3$：核心部分，按题意模拟，要注意$x$和$y$要倒过来。
```
if(t=='N')//向上走,外围是a[y][x]
	M[y][x]='X',y++;
if(t=='S')//向下走，外围是a[y-1][x-1]
	M[y-1][x-1]='X',y--;
if(t=='W')//向左走，外围是a[y][x-1]
	M[y][x-1]='X',x--;
if(t=='E')//向右走，外围是a[y-1][x]
	M[y-1][x]='X',x++;
if(t=='.')		
	break;
 
```


------------

$step$ $4$：处理输出，注意每一行都要换行，倒序输出。
```
void OT() {
	for(int i=31;i>=0;i--,puts(""))
		for(int j=0;j<=31;j++)
			cout<<M[i][j];
	puts("");
}
```

------------

为了不占用篇幅，[完整代码](https://www.luogu.com.cn/paste/32ta5hnq)就扔这里了，自取。