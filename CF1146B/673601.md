## [题目传送门](https://www.luogu.com.cn/problem/CF1146B)
# 思路
设字符串 $t$ 去掉所有的字符 ```a``` 后的字符串为 $tmp$，则显然 $tmp=s'+s'$，将 $tmp$ 劈成两半取一半就是 $s'$，所以 $s=t-s'$。

无解的情况:

- $tmp$ 长度为奇数
- $tmp$ 前半部分和后半部分不相同
- $t$ 的后半部分含有 ```a``` （即 $s'$ 含有 ```a```）。
- 上述情况直接输出 ```:(```。

代码就不给了，思路已经讲的很明白了（~~我不说因为我懒~~）。