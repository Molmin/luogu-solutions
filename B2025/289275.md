直接按照题目要求输出即可。

对于第 $1,5$ 行：$2$ 个空格加一个 `*`。

对于第 $2,4$ 行：$1$ 个空格加三个 `*`。

对于第 $3$ 行：直接输出五个 `*` 即可。

代码：

```cpp
#include<bits/stdc++.h>
using namespace std;
double f;
int main(){
	cout<<"  *"<<endl;
	cout<<" ***"<<endl;
	cout<<"*****"<<endl;
	cout<<" ***"<<endl;
	cout<<"  *"<<endl;
}
```
