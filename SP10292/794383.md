## 题意
有 `left`，`center`，`right` 三个杯子，一开始告诉你有一个小球在哪个杯子里，并告诉你 $n$ 个交换杯子的操作，问最后小球在哪个杯子里。
## 思路
先使用 `map` 将三个英文单词离散化成三个数字，使用一个数组来记录小球的位置，每读入一次操作就对数组对应位置进行 `swap`，最后看小球在哪个杯子里就行了。

然而你会 WA 在第 10 个点，这是因为这个点输入不合法，参见[前人的经验](https://www.luogu.com.cn/discuss/147919)。