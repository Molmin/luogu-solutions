1. 若序列中出现过 $0$，将其余所有数都与 $0$ 取 $\min$ 即可将序列全部变为 $0$，操作次数为 $n-num_0$。

2. 没有出现过 $0$，但若序列中存在两个相同的数，用一次操作 $1$ 即可把其中一个变为 $0$，然后与情况 $1$ 相同，操作次数为 $n$。

3. 没有出现过 $0$，也没有两个相同的数，用一次操作 $2$ 就会出现两个相同的数，然后与情况 $2$ 相同，操作次数为 $n+1$。

由于一次操作最多将一个非零数变为 $0$，故可得上述为最优方案。