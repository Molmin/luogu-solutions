## 001 做法

我们发现，因为 $7<10$，所以只改变个位，一定能够将其变成 $7$ 的倍数，即答案 $\le 1$。

于是分类讨论：

### 如果 $n$ 是 $7$ 的倍数

直接输出 $n$。

### 如果 $n$ 不是 $7$ 的倍数

枚举个位。

## 002 代码

```c++
void run(){
	int n;
	cin>>n;
	if(n%7==0){
		cout<<n<<"\n";
		return;
	}
	n=n/10*10;
	while(n%7)n++;
	cout<<n<<"\n";
}
```