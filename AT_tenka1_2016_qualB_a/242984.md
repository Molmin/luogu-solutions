### 思路&解析
定义函数$f(n)=floor((n^2+4.0)/8.0)$，输出$f(f(f(20)))$ 即可。

其实呢，按照公式模拟一遍可知这个答案为$12246$。如果代码想要简洁一些，直接输出 $12246$ 就好啦。


------------
### 代码（```Go```）：
```
package main
import "fmt"
func main() {
    fmt.Println(12246)
}
```
