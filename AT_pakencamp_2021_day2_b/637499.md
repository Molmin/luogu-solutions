## 题意:

#### 输入两个正整数 $a$ 和 $b$ ，输出 $a^2*$ $b$ 的值。

## 分析:

#### 依照题意模拟即可。

 $Code:$

```cpp
#include<bits/stdc++.h>

using namespace std;
int main()
{
	int a, b;
	cin >> a >> b;
	cout << a * a * b << endl;
	return 0;
}
```
