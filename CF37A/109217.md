看到那么多大佬简短的代码，~~我真是无地自容了。~~

二话不说上代码（内含注释，为维护和平，本题解为**高级防Ctrl+C、Ctrl+V代码**）


------------


```cpp
#<iostream>//输入输出头文件
#<algorithm>//sort头文件
using namespace std;//名字空间
main()//主函数
{
	cin>>n；//输入数据
	c=n；//为方便后面统计总数，找n的替身
	for(i=0；i<n；i++)//输入数据
	{
		cin>>a[i]；
	}
	sort(a，a+n)；//第一次进行排序
	for(i=0；i<n；i++)//用桶统计高度
	{
		b[a[i]]++；
	}
	for(i=1；i<n；i++)//去重
	{
		if(a[i]==a[i-1])
		{
			a[i-1]=12345；//用一个较大的数排除
			c--；//减去总数
		}
	}
	sort(b，b+1010)；//第二次排序
	cout<<b[1009]<<' '<<c<<endl；输出最大的高度和塔的总数
	return 0；
}
```
