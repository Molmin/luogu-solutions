简单的字符串判断。而且只有一个字母错，就比较好办。

因为弟弟的错误单词长度不变，所以有以下两种情况：
- $len = 3\ (one,\ two)$。
- $len = 5\ (three)$。

所以可以先判断长度是否为 $5$，如果是，直接输出 $3$。而如果长度是 $3$，可以这样判断：如果 $s_0 ≠$ `o`、$s_1 ≠$ `n`、$s_2 ≠$ `e` 这 $3$ 项条件中仅成立 $1$ 项，输出 $1$，否则输出 $2$。