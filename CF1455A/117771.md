$\text{Educational Round}$ 的送分题（也是我唯一 $\text{AC}$ 的题目   
讲一下思路：      
$f(i)$ 代表将 $i$ 依次翻转去掉前缀 0    
那么 $f(f(i))$ 就是 $i$ 去掉后缀 0    
code:   
```cpp
#include<bits/stdc++.h>
using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
		string s;
		cin>>s;
		cout<<s.length()
	}
}
```
