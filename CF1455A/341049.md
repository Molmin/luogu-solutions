### 找规律的一题

如果函数$f(x)$代表$x$这个数取反，那么$f(f(x))$就是只去掉$x$的末尾0（举个例子，$f(f(1000))=1$）。

知道这个后，这道题就变得很水了。对于函数 $g(x)=i/f(f(x))$，它的结果就会是数 $x$ 的长度 $l$。我们就只需要输入一个字符串，然后再用 $len$ 方法将长度输出（~~是不是比P1000还简单~~）。

AC代码：
```python
t=int(input())
for i in range(0,t):
    s=input()
    print(len(s))#输出长度
```
