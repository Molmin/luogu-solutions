		这道题的题意。。。可能许多人没读懂，我也是其中的一位，现在懂了，发现没人发过这一点，就发一下吧。
        需要注意的是，所有输入的字符串（每一行输入的字符串）是一场（不是一局）比赛中的，而不是一行字符串表示一场比赛！所以最后要转变成一行字符串来处理（指的是大多数人的思路，特殊的话就例外），不要以为是一行一局比赛（我一开始就这么想，因此全WA）。
        发出来让大家注意一下，代码就不贴了。
