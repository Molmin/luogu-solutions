
```cpp
#include <iostream>
#include <cstdio>
using namespace std;
int main() {
    int a11[100000] = {0}, b11[100000] = {0}, s11 = 0, a21[100000] = {0}, b21[100000] = {0}, s21 = 0;  _//数组顶大一点，以防万一_
                                                                                                                                                               _//s11, s21 分别存储两种赛制比赛局数_
                                                                                                                         _//a11[i], b11[i], a21[i], b21[i] 分别存储第i局两种赛制中两人的分数_
    while (1) {      _//先死循环，再在循环内判断是否退出_
        char ch;
        cin >> ch;
        if (ch == 'E') {      _//若读到结束符，则打印并退出_
            for (int i = 0; i <= s11; i++) {
```
\_//这里循环变量从0循环到s11是因为a11，b11，a21，b21是从下标0开始存储的，最后一局下表为s11和s21\_
```cpp
                cout << a11[i] << ":" << b11[i] << endl;
            }
            cout << endl;
            for (int i = 0; i <= s21; i++) {
                cout << a21[i] << ":" << b21[i] << endl;
            }
            return 0;
        } else {
            if (ch == 'W') {
                a11[s11]++;
                a21[s21]++;
            } else {
                b11[s11]++;
                b21[s21]++;
            }
            if ((a11[s11] >= 11 || b11[s11] >= 11) && (a11[s11]-b11[s11] >= 2 || a11[s11]-b11[s11] <= -2）{
```
\_//后面两个条件相当于abs（a11[s11]-b11[s11]），下面同理\_
```cpp
                s11++;
            }
            if ((a21[s21] >= 21 || b21[s21] >= 21) && (a21[s21]-b21[s21] >= 2 || a21[s21]-b21[s21] <= -2)) {
                s21++;
            }
        }
    }
}
```