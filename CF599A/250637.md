不知道为什么题解这么少。

题意明确，就是求到两个点再回到出发点的最短距离。

正常走，直接家->超市1->超市2肯定珂以，但不一定是最短。

**为什么不一定？唯一的可能性是绕路会更短！！！**

缩以把每条路长度变为min(这条路长度，另两条路长度总和).

然后直接三边长度一加就Over了。

上代码（juruo马蜂很烂求不要吐槽）：
```py
a,b,c=input().split()
a=int(a)
b=int(b)
c=int(c)
#输入占了大部分······
print(min(a,b+c)+min(b,a+c)+min(c,b+a))
```