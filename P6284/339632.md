这题没人用 PY？

其实这题就是为 PY 而生的（至少我是这么认为的）。

为什么呢？这道题满足以下两个特点：

- 不卡常（PY 比较慢）；
- 输入都是一个数一行（不用打那个长长的`map`函数）。

而 PY 的代码量小，可以秒 A 这道题。

我们来看思路。

要求第$n+1$月可用的流量，我们可以用

$n+1$月总共得到的流量$-$前$n$个月总共用去的流量

这个公式进行计算。

第二部分非常简单，直接边输入边减即可；

第一部分相当于每月的流量$x \times$总共$n+1$个月。

那么代码就很好得到了。先来看代码。

```python
x=int(input()) #input函数自动输入一行一个数
n=int(input())
ans=(n+1)*x #先算总流量
for i in range(n): #n次执行
    m=int(input()) #输入每一个月的流量
    ans-=m #从总流量里减去
print(ans) #输出答案
```

最后，这题我必须要吐槽一下。

- 数据太弱

居然连最终没流量了，$<0$的都没有。。。

- 题目描述模糊不清

题目中：

> 每月未用完的流量，可以在次月继续使用。

那么从再前一个月留下来的流量，次月还可以用吗？

当然我确实可能有点钻牛角尖了，但是由于中国流量的背景在，比较容易让人产生误解。

好了，最后还是希望这篇题解对大家有帮助，求赞！