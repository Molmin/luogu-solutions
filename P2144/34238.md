递推式楼下已经讲得很清楚了

一个变形的斐波那契数列

f[0]=1,f[1]=3,f[n]=f[n-1]+f[n-2]

然后F[n]=f[n]^2-4\*((n+1)%2)

至于高精就用py水过了

# 读入n

    n=input()

# 初始化

    f=[1,3]

# i循环到n，计算f[i]

    for i in range(2,n):

        f.append(f[i-1]+f[i-2])

# 输出F[n]

print(f[n-1]\*f[n-1]-4\*((n+1)%2))
