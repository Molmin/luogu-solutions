2^32-1用unsigned即可；前面那些用栈和字符串的把我吓到了……

```cpp

#include<iostream>
using namespace std;
unsigned a[100];
char i;
int main()
{
    while(cin>>a[i],a[i++]);
    for(i-=2;i>=0;i--)cout<<a[i]<<' ';
    
    return 0;
}

```