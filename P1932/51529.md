luogu好像支持了`python`的样子。。`python`作为一个自带高精的语言。。emmmmmmm

以下是`python3`的代码，需要注意的一点就是`input()`函数一次读入一行，返回一个字符串，要先转成`int`

``` python
a = int(input())
b = int(input())
print(a + b)
print(a - b)
print(a * b)
print(a // b)
print(a % b)
```