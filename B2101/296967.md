本题可以创建一个二维数组，将边缘加起来求和。

------------
但是我们要~~搞事情~~研究最优解。

考虑不用数组，用双重循环，判断到边界，直接加上去，即可。
```cpp
for (int i = 1; i <= m; i++)
    for (int j = 1; j <= n; j++) {
        scanf("%d", &tmp);
        if (i == 1 || j == 1 || i == m || j == n) ans += tmp;
    }
```