# 题解
1. 用字符串读入，特判 $\texttt{\{\}}$ 的情况；  
2. 将读入的字符转化为数为止，即 `x=x*10+s[i]-'0'`;
3. 若遇到 $\texttt{,}$ 或 $\texttt{\}}$，将 $x$ 转化为十进制和十六进制比较长度即可；  

放上转化为十六进制的代码：
```cpp
while(x)
{
    if(x%16<10) s+=x%10+'0';
    else s+=x%10+'7';
    x>>=4;
}
s="0x"+s;
```