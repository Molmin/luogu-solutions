~~c++党来做一道P党的题目~~

首先，$10^{100}$ 的数据规模，c++的 $long long$ 显然是不能过的，高精度又太麻烦了，所以用了Python。

设数量和为 $a$ ，数量差为 $b$,两个数为 $x,y$ ($ab$为参数)则：

$$x+y=a,x-y=b$$

然后用加减消元法两个式子加在一起，即

$$x+y+x-y=a+b$$

然后；

$2x=a+b$

所以：

$x=(a+b)/2$

同理可得（用减法）：

$y=(a-b)/2$

然后代码实现一下就行啦！

代码就不放了，参照其他大佬的就行了。
