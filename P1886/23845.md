只给思路，程序自己打。给了思路基本上也可以想出程序了。


法1：


维护两个大小为K的堆（大根堆和小根堆），使得里面的元素都是窗口内的元素。


效率分析：


......维护堆自己尝试吧


法2：利用单调队列


说明：


始终维护一个下标递增、值也递增的队列。当扫描到第 i 个数时，将队头下标<=i-k的数删掉（把入队时间太长的给删掉，因为已经没有用了），同时将第 i个数插入队尾（的适当处）此时队头元素即为所求最小值。（GG，LOL,Orz）


效率分析：


O(n)，跟堆产生鲜明的对比。
