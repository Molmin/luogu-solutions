这道题是一个非常简单的去重问题。

------------
因为这个题目数据范围较小，考虑使用一个 bool 类型数组，表示一个数有没有出现。如果没出现，输出；如果出现过，就跳过，即可。
```cpp
for (int i = 1; i <= n; i++) {
    cin >> tmp;
    if (!v[tmp]) v[tmp] = true, printf("%d ", tmp);
}
```