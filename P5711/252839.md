这题作为入门赛第2题，难度还是很水的。

就让你判断n是否为闰年，是输出1，否则输出0。

只需要理解闰年是什么就可以了。（自己问度娘去）

判断闰年方法：是4的倍数但不是100的倍数，或者是400的倍数。

参考代码：
```cpp
#include<bits/stdc++.h>
using namespace std;
int n;
int main()
{
	cin>>n;
	cout<<((n%4==0&&n%100!=0)||(n%400==0))?1:0;
	return 0;
} 
```

很简洁对吧！